<%= simple_form_for(@case_request) do |f| %>
  <%= f.hidden_field :form_identifier, value: "case_request_form_2" %>

  <%= f.error_notification %>
  <%= f.error_notification message: f.object.errors[:base].to_sentence if f.object.errors[:base].present? %>

    <div class="form-inputs" data-controller="dependent-select">
      <div class="row">
        <div class="col-md-6">
          <%= f.input :service_region, 
                    label_html: { class: 'form-label' },
                    input_html: { class: 'form-control' } %>
        </div>
        <div class="col-md-6">
          <%= f.input :location, 
                    label_html: { class: 'form-label' },
                    input_html: { class: 'form-control' } %>
        </div>
      </div>
    </div>

    <div class="form-actions">
      <%= f.button :submit, class: "btn btn-sm d-inline-flex btn-neutral" %>
  </div>
<% end %>
